<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="1dp"
    app:strokeColor="#E0E0E0"
    android:clickable="true"
    android:focusable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon Circle -->
        <androidx.cardview.widget.CardView
            android:id="@+id/iconContainer"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:cardCornerRadius="20dp"
            app:cardBackgroundColor="#FFE5E5"
            app:cardElevation="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_flask"
                app:tint="#FF5252" />

        </androidx.cardview.widget.CardView>

        <!-- Test Type Label -->
        <TextView
            android:id="@+id/tvTestType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Blood Test"
            android:textSize="11sp"
            android:textColor="#757575"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/iconContainer"
            app:layout_constraintTop_toTopOf="@id/iconContainer" />

        <!-- Test Name -->
        <TextView
            android:id="@+id/tvTestName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Complete Blood Count (CBC)"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/iconContainer"
            app:layout_constraintEnd_toStartOf="@id/btnMenu"
            app:layout_constraintTop_toBottomOf="@id/tvTestType" />

        <!-- Lab Icon + Lab Name -->
        <ImageView
            android:id="@+id/icLab"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_hospital"
            app:tint="#757575"
            android:layout_marginTop="8dp"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/iconContainer"
            app:layout_constraintTop_toBottomOf="@id/tvTestName" />

        <TextView
            android:id="@+id/tvLabName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Apollo Diagnostics"
            android:textSize="12sp"
            android:textColor="#757575"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/icLab"
            app:layout_constraintEnd_toStartOf="@id/btnMenu"
            app:layout_constraintTop_toTopOf="@id/icLab"
            app:layout_constraintBottom_toBottomOf="@id/icLab" />

        <!-- Doctor Icon + Doctor Name -->
        <ImageView
            android:id="@+id/icDoctor"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_person"
            app:tint="#757575"
            android:layout_marginTop="4dp"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/iconContainer"
            app:layout_constraintTop_toBottomOf="@id/icLab" />

        <TextView
            android:id="@+id/tvDoctorName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Dr. Priya Sharma"
            android:textSize="12sp"
            android:textColor="#757575"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/icDoctor"
            app:layout_constraintEnd_toStartOf="@id/btnMenu"
            app:layout_constraintTop_toTopOf="@id/icDoctor"
            app:layout_constraintBottom_toBottomOf="@id/icDoctor" />

        <!-- Date -->
        <TextView
            android:id="@+id/tvDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="20/2/2025"
            android:textSize="11sp"
            android:textColor="#9E9E9E"
            android:layout_marginTop="4dp"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/iconContainer"
            app:layout_constraintTop_toBottomOf="@id/icDoctor" />

        <!-- Menu Button (Top Right) -->
        <ImageView
            android:id="@+id/btnMenu"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_more_vert"
            app:tint="#757575"
            android:padding="2dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
